FROM python:3.11-slim

# 不要なredis-serverのインストールを削除し、最小限のパッケージ構成にします
WORKDIR /app

# 依存関係のインストール
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# ソースコードのコピー
COPY . .

# entrypoint.shを介さず、直接Pythonを実行することでシグナル処理を安定させます
CMD ["python", "main.py"]