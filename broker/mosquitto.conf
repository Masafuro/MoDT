# 1883ポートで待ち受け
listener 1883
# 外部公開せず内部のみの想定なので、まずは匿名接続を許可
allow_anonymous true

# ログとデータの保存先（Docker内のパス）
persistence true
persistence_location /mosquitto/data/
log_dest file /mosquitto/log/mosquitto.log
log_dest stdout